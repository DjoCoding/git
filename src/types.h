#ifndef TYPES_H_
#define TYPES_H_

#include <stdio.h>
#include <stdint.h>

typedef size_t   usize;
typedef int64_t  i64;
typedef uint64_t u64;
typedef uint32_t u32;
typedef uint16_t u16;
typedef uint8_t  u8;

#endif // TYPES_H_